--- a/src/ekaf_utils.erl
+++ b/src/ekaf_utils.erl
@@ -58,7 +58,7 @@
     B.
 
 btoi(B) when is_binary(B) ->
-    binary_to_integer(B);
+    list_to_integer(binary_to_list(B));
 btoi(I) when is_list(I)->
     list_to_integer(I);
 btoi(I) when is_integer(I)->
